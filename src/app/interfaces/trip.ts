import {Site} from './site';
import {Country} from './country';

export interface Trip {
  id: string;
  tripName: string;
  // tripDate: any;
  // team: any;
  // organization: any;
  // site: Site;
  // country: Country;
}
